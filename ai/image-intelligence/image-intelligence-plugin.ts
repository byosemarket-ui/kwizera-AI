import type { AiCoreManager } from "../core/ai-core-manager.js";
import type { AiModulePlugin } from "../core/types.js";
import { ImageIntelligenceManager } from "./image-intelligence-manager.js";
export function createImageIntelligencePlugin(manager: ImageIntelligenceManager, _core: AiCoreManager): AiModulePlugin { return { id: "image-intelligence-runtime", name: "KWIZERA AI Image Intelligence Runtime", version: "0.1.0", async initialize() { if (!manager.isInitialized()) throw new Error("Image Intelligence Manager is not initialized"); }, async shutdown() { /* profiles remain durable */ }, async healthCheck() { return { healthy: manager.isInitialized(), message: "Image intelligence runtime operational" }; } }; }