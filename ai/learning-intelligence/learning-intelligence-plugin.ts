import type { AiCoreManager } from "../core/ai-core-manager.js";
import type { AiModulePlugin } from "../core/types.js";
import { AiLearningManager } from "./learning-intelligence-manager.js";
export function createLearningIntelligencePlugin(manager: AiLearningManager, _core: AiCoreManager): AiModulePlugin { return { id: "learning-intelligence-runtime", name: "KWIZERA AI Learning Intelligence Runtime", version: "0.1.0", async initialize() { if (!manager.isInitialized()) throw new Error("AI Learning Manager is not initialized"); }, async shutdown() { /* durable learning remains available after shutdown */ }, async healthCheck() { return { healthy: manager.isInitialized(), message: "AI learning intelligence runtime operational" }; } }; }