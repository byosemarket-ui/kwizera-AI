import type { AiCoreManager } from "../core/ai-core-manager.js";
import type { AiModulePlugin } from "../core/types.js";
import { MarketingIntelligenceManager } from "./marketing-intelligence-manager.js";
export function createMarketingIntelligencePlugin(manager: MarketingIntelligenceManager, _core: AiCoreManager): AiModulePlugin { return { id: "marketing-intelligence-runtime", name: "KWIZERA AI Marketing Intelligence Runtime", version: "0.1.0", async initialize() { if (!manager.isInitialized()) throw new Error("Marketing Intelligence Manager is not initialized"); }, async shutdown() { /* profiles remain durable */ }, async healthCheck() { return { healthy: manager.isInitialized(), message: "Marketing intelligence runtime operational" }; } }; }