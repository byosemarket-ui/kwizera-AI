export type DecisionTaskKind = "pipeline" | "image-generation" | "video-generation" | "marketing" | "analysis";
export interface DecisionOption { id: string; label: string; workflow: string; creativeApproach: string; renderingStrategy: string; score: number; risk: "low" | "medium" | "high"; resourceCost: "low" | "medium" | "high"; reasons: string[]; }
export interface DecisionIntelligenceProfile { id: string; projectId: string; taskKind: DecisionTaskKind; objective: string; priority: "critical" | "high" | "normal" | "low"; options: DecisionOption[]; selected: DecisionOption; confidence: number; resourceAnalysis: { availableRamMb: number; availableStorageMb: number; gpuAvailable: boolean; recommendation: string }; modelRecommendation: { id: string; name: string; category: string; reason: string }; explanation: string; risks: string[]; learnedFrom: string[]; createdAt: string; cached: boolean; }
export interface DecisionIntelligenceStore { profiles: DecisionIntelligenceProfile[]; history: Array<{ id: string; at: string; projectId: string; event: string; detail: string }>; cache: Record<string, string>; logs: Array<{ at: string; level: "info" | "warning" | "error"; message: string }>; }